{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run hexo server",
            "type": "shell",
            "command": "hexo server",
            "runOptions": {
                "runOn": "folderOpen"
            }
        }
    ]
}
